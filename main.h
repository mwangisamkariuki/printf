#ifndef MAIN_H
#define MAIN_H

#include <stdlib.h>
#include <stdio.h>
#include <limits.h>
#include <stdargs.h>

int _putchar(char c);
int printf(const char *format, ...);
int print_string(char *);

#endif